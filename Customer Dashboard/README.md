# Customer Dashboard

Desktop-first CRM revenue dashboard built with Next.js App Router and shadcn-style UI primitives.

## Run locally

```bash
cd "/Users/sdwyer/Documents/New project/april-26-release-notes/Customer Dashboard"
npm run dev
```

This subproject reuses the workspace's existing `node_modules`, so no extra install step is needed in this environment.
