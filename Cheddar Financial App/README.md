# Cheddar Financial App

Standalone Next.js homescreen prototype recreated from the Cheddar Financial App Figma file.

## Run locally

```bash
cd "/Users/sdwyer/Documents/New project/cheddar-financial-app"
npm run dev
```

This subproject reuses the workspace's existing `node_modules`, so no extra install step is needed in this environment.
